<h1>{{title}}</h1>

{{#each bookings}}

    <article>
        <h2>Reserva {{_id}}</h2>

        <p>Cliente: {{clientName}}</p>
        <p>Fecha: {{date}}</p>
    </article>

{{else}}

    <p>No hay reservas registradas.</p>

{{/each}}